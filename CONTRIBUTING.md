# Contributing to AgentDock

Thanks for helping build the open command center for coding agents.

1. Create a focused issue before a large change.
2. Fork the repository and branch from `main`.
3. Run `npm install`, `npm run lint`, and `npm run build`.
4. Open a small pull request with screenshots for interface changes.

Be kind, explain trade-offs, and never commit API keys or user prompts.
